import { __awaiter } from "tslib";
import { execa } from '../utils';
const SHELL_COMMAND = 'cat /var/lib/dbus/machine-id /etc/machine-id 2> /dev/null';
const parse = raw => raw
    .toString()
    .replace(/\r+|\n+|\s+/gi, '')
    .toLowerCase();
export const linuxHWID = () => __awaiter(void 0, void 0, void 0, function* () { return execa(SHELL_COMMAND, parse); });
