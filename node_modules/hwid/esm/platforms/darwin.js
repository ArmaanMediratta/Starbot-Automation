import { __awaiter } from "tslib";
import { execa } from '../utils';
const SHELL_COMMAND = 'ioreg -rd1 -c IOPlatformExpertDevice';
const parse = raw => raw
    .split('IOPlatformUUID')[1]
    .split('\n')[0]
    .replace(/=|\s+|"/gi, '')
    .toLowerCase();
export const darwinHWID = () => __awaiter(void 0, void 0, void 0, function* () { return execa(SHELL_COMMAND, parse); });
